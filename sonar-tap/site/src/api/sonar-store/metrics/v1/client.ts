// Generated by gve api generate. Do not edit manually.

import type { QueryMetricsRequest, QueryMetricsResponse, ReportMetricsRequest, ReportMetricsResponse } from "./types";

/** Options for customizing client behavior. */
interface ClientOptions {
  fetch?: typeof globalThis.fetch;
  baseHeaders?: Record<string, string>;
  onError?: (error: Error, method: string) => void;
}

export class MetricsServiceClient {
  private readonly baseUrl: string;
  private readonly options: ClientOptions;

  constructor(baseUrl: string, options?: ClientOptions) {
    this.baseUrl = baseUrl;
    this.options = options ?? {};
  }

  async ReportMetrics(reqBody: ReportMetricsRequest): Promise<ReportMetricsResponse> {
    const fetchFn = this.options.fetch ?? globalThis.fetch;
    const response = await fetchFn(`${this.baseUrl}/ReportMetrics`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...this.options.baseHeaders,
      },
      body: JSON.stringify(reqBody),
    });

    if (!response.ok) {
      const text = await response.text();
      const error = new Error(`request failed: status=${response.status}, body=${text}`);
      this.options.onError?.(error, "ReportMetrics");
      throw error;
    }

    return (await response.json()) as ReportMetricsResponse;
  }

  async QueryMetrics(reqBody: QueryMetricsRequest): Promise<QueryMetricsResponse> {
    const fetchFn = this.options.fetch ?? globalThis.fetch;
    const response = await fetchFn(`${this.baseUrl}/QueryMetrics`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        ...this.options.baseHeaders,
      },
      body: JSON.stringify(reqBody),
    });

    if (!response.ok) {
      const text = await response.text();
      const error = new Error(`request failed: status=${response.status}, body=${text}`);
      this.options.onError?.(error, "QueryMetrics");
      throw error;
    }

    return (await response.json()) as QueryMetricsResponse;
  }
}
