// Code generated by thriftgo (0.4.2). DO NOT EDIT.
// apis/monitor_hub/base/v1/base.thrift
namespace go monitor_hub.base.v1

// 通用响应结构
struct Response {
    1: list<string> data (go.tag = "json:\"data,omitempty\"");       // 数据 (JSON字符串列表)
    2: string msg (go.tag = "json:\"msg,omitempty\"");              // 响应消息
    3: i64 code (go.tag = "json:\"code\"");               // 响应码，0表示成功（这个通常不省略）
    4: Page page (go.tag = "json:\"page,omitempty\"");              // 分页信息 (可选)
}

// 分页返回结构
struct Page {
    1: i64 total (go.tag = "json:\"total\"");              // 总数据量
    2: i64 page (go.tag = "json:\"page,omitempty\"");               // 当前页码
    3: i64 page_size (go.tag = "json:\"page_size,omitempty\"");          // 每页大小
    4: i64 total_page (go.tag = "json:\"total_page,omitempty\"");         // 总页数
    5: i64 num (go.tag = "json:\"num,omitempty\"");                // 当前页实际数量
    6: string query (go.tag = "json:\"query,omitempty\"");           // 查询条件 (可选)
    7: string projection (go.tag = "json:\"projection,omitempty\"");      // 投影字段 (可选)
    8: string distinct (go.tag = "json:\"distinct,omitempty\"");        // 去重字段 (可选)
}

// 通用查询请求
struct QueryRequest {
    1: i64 page (go.tag = "json:\"page\" form:\"page\"");               // 当前页码
    2: i64 page_size (go.tag = "json:\"page_size\" form:\"page_size\"");          // 每页大小
    3: string query (go.tag = "json:\"query,omitempty\" form:\"query\"");           // 查询条件 (可选)
    4: string projection (go.tag = "json:\"projection,omitempty\" form:\"projection\"");      // 投影字段 (可选)
    5: string distinct (go.tag = "json:\"distinct,omitempty\" form:\"distinct\"");        // 去重字段 (可选)
}

// 通用ID请求
struct IdRequest {
    1: string id (go.tag = "json:\"id\ api.path:\"/:id\"");               // ID
}

// Token信息（认证相关）
struct TokenInfo {
    1: string access_token (go.tag = "bson:\"access_token\""),                     // 访问令牌
    2: string refresh_token (go.tag = "bson:\"refresh_token\""),                    // 刷新令牌
    3: i64 expires_at (go.tag = "bson:\"expires_at\""),                          // 过期时间戳
    4: i64 refresh_expires_at (go.tag = "bson:\"refresh_expires_at\""),                  // 刷新令牌过期时间戳
    5: string token_type (go.tag = "bson:\"token_type\"")                        // 令牌类型（通常为"Bearer"）
}

// 通用错误码枚举
enum BaseErrorCode {
    // 通用错误 (1000-1099)
    INVALID_PARAM = 1001,           // 参数无效
    MISSING_PARAM = 1002,           // 缺少必要参数
    INVALID_REQUEST = 1003,         // 请求格式错误
    UNAUTHORIZED = 1004,            // 未授权
    FORBIDDEN = 1005,               // 禁止访问
    NOT_FOUND = 1006,               // 资源不存在
    INTERNAL_ERROR = 1007,          // 内部错误
    SERVICE_UNAVAILABLE = 1008,     // 服务不可用
    RATE_LIMIT_EXCEEDED = 1009,     // 请求频率超限
    RESOURCE_ALREADY_EXISTS = 1010, // 资源已存在
}
