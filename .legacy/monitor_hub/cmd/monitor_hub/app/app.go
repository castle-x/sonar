// Code generated by hzx generator. DO NOT EDIT.
package app

import (
	"context"
	"sync"

	configV1 "monitor_hub/config/v1"
	"monitor_hub/internal/hzapp"

	"git.woa.com/castlexu/goutils/ablog"
	"github.com/cloudwego/hertz/pkg/route"
	"github.com/spf13/cobra"
	"github.com/spf13/pflag"
)

var configFilePath string

// AppContext 应用上下文，包含 Context 和 Cancel 函数
type AppContext struct {
	Context context.Context
	Cancel  context.CancelFunc
}

type Monitor_hubApp struct {
	logger *ablog.Logger
	hzapp  *hzapp.HertzApp
	cfg    *configV1.Config
	cancel context.CancelFunc
	wg     *sync.WaitGroup
}

func initCobraFlags(fs *pflag.FlagSet) {
	fs.StringVarP(&configFilePath, "config", "c", "config.yaml", "monitor_hub config file .")
}

func newMonitor_hubApp(
	appCtx *AppContext,
	cfg *configV1.Config,
	wg *sync.WaitGroup,
	hzapp *hzapp.HertzApp,
) *Monitor_hubApp {
	return &Monitor_hubApp{
		logger: ablog.NewLogger("monitor_hub-app"),
		hzapp:  hzapp,
		cfg:    cfg,
		cancel: appCtx.Cancel, // 从 AppContext 中获取 cancel 函数
		wg:     wg,
	}
}

func NewServerCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:  "monitor_hub",
		Long: "Hertz Micro Service for Http Server",
		RunE: func(cmd *cobra.Command, args []string) error {
			return Run()
		},
		Args: func(cmd *cobra.Command, args []string) error {
			return nil
		},
	}
	initCobraFlags(cmd.PersistentFlags())
	return cmd
}

func Run() error {
	app, err := New(configFilePath)
	if err != nil {
		return err
	}
	app.registerShutdownFunction()
	return app.hzapp.Run()
}

func (a *Monitor_hubApp) registerShutdownFunction() {
	a.hzapp.RegisterShutdown(
		route.CtxCallback(func(ctx context.Context) {
			a.logger.Info("Shutting down application...")

			// 1. 取消 context，触发所有触发器停止
			if a.cancel != nil {
				a.cancel()
				a.logger.Info("Cancelled context, waiting for triggers to stop...")
			}

			// 2. 等待所有触发器的 goroutine 结束 TODO 可能需要增加超时检测
			a.wg.Wait()
			a.logger.Info("All triggers stopped")

			// 2. 关闭存储
			a.hzapp.GetCustomDeps().AggregatedPointStorage.Close()
			
			// 3. 优雅关闭由 HzApp 统一处理（WebSocket 等）
			a.logger.Info("Application shutdown complete")
		}),
	)
}

func (a *Monitor_hubApp) PrepareRun(ctx context.Context) error {
	return nil
}
